--- a/dbms/tests/clickhouse-test
+++ b/dbms/tests/clickhouse-test
@@ -58,7 +58,7 @@ def main(args):
 
 	if args.zookeeper is None:
 		try:
-			check_call(['grep', '-q', '<zookeeper', '/etc/clickhouse-server/config-preprocessed.xml'], )
+			check_call(['grep', '-q', '<zookeeper', '../../debian/clickhouse-server-config-tests-preprocessed.xml'], )
 			args.zookeeper = True
 		except CalledProcessError:
 			args.zookeeper = False
--- a/dbms/tests/queries/0_stateless/00383_utf8_validation.reference
+++ /dev/null
@@ -1,16 +0,0 @@
-{
-	"meta":
-	[
-		{
-			"name": "s",
-			"type": "String"
-		}
-	],
-
-	"data":
-	[
-		["� �"]
-	],
-
-	"rows": 1
-}
--- a/dbms/tests/queries/0_stateless/00383_utf8_validation.sql
+++ /dev/null
@@ -1,2 +0,0 @@
-SET output_format_write_statistics = 0;
-SELECT '\xED\x20\xA8' AS s FORMAT JSONCompact;
